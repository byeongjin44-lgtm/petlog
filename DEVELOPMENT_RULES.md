\# petLog 개발 운영 규칙



\## 1. 프로젝트 기준



\- 항상 GitHub의 `PROJECT\_STATE.md`와 현재 코드를 기준으로 현재 상태를 먼저 확인한다.

\- 이미 구현된 기능을 다시 만들지 않는다.

\- 기존 UI와 구조를 최대한 유지하고 필요한 범위만 수정한다.

\- 작업 시작 전 현재 로컬 코드와 GitHub 상태가 일치하는지 확인한다.



\## 2. 개발 진행 방식



\- 한 번에 큰 기능을 만들지 않고 작은 단위로 구현 → 실행 → 확인한다.

\- 사용자가 실제 개발 서버에서 확인할 수 있는 단계마다 테스트한다.

\- 추측으로 원인을 단정하지 않는다.

\- 오류가 발생하면 실제 오류 메시지와 코드를 먼저 확인한다.

\- 문제 해결 전까지 관련 없는 코드를 함께 수정하지 않는다.



\## 3. 오류 처리 원칙



\- 개발 단계에서는 Supabase, Next.js 등의 실제 오류 메시지를 최대한 그대로 노출한다.

\- 예:

&#x20; `setErrorMessage(error.message)`

\- 오류 원인을 확인하고 해결한 뒤, 서비스 출시 단계에서 사용자 친화적인 메시지로 변환한다.

\- 임의의 에러 메시지로 실제 오류를 숨기지 않는다.



\## 4. Supabase 개발 원칙



\- Supabase 등 외부 서비스 호출 결과의 `error`를 먼저 확인한다.

\- 인증 기능은 회원가입 → 로그인 → 세션 유지 → 로그아웃 순서로 검증한다.

\- 개발 중에는 실제 Supabase Dashboard 상태와 애플리케이션 동작을 함께 확인한다.

\- 비밀키 및 환경변수 값은 코드나 Git 커밋에 직접 노출하지 않는다.



\## 5. 테스트 원칙



\- 기능 구현 후 반드시 실제 `localhost:3000`에서 확인한다.

\- 회원가입/로그인처럼 상태가 중요한 기능은 성공뿐 아니라 실패 상황도 테스트한다.

\- 새로고침, 페이지 이동 등 실제 사용자 행동을 기준으로 상태 유지 여부를 확인한다.



\## 6. Git 원칙



\- 하나의 작업 단위가 완료되면 의미 있는 커밋 메시지로 기록한다.

\- 작업 중인 변경사항을 임의로 삭제하거나 되돌리지 않는다.

\- `PROJECT\_STATE.md`는 실제 구현 상태와 항상 일치하도록 갱신한다.

\- 프로젝트의 중요한 개발 방향이나 협업 규칙이 변경되면 이 문서를 함께 갱신한다.



\## 7. AI 협업 원칙



\- AI는 기존 코드를 먼저 확인한 뒤 수정안을 제시한다.

\- AI는 코드 전체를 불필요하게 재작성하지 않는다.

\- 사용자가 이해할 수 있도록 현재 상태, 변경 이유, 다음 테스트 단계를 명확하게 설명한다.

\- 개발 과정에서 합의된 중요한 원칙은 이 문서에 기록하여 새 대화에서도 동일하게 적용한다.

\- 중간에 합의되지 않은 기능이나 구조를 임의로 추가하지 않는다.

\- 사용자가 "STOP" 또는 "잠깐"이라고 하면 현재 작업을 중단하고 상태부터 정리한다.

